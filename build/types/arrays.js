const numeros = [1, 2, 3, 4, 5];
numeros.push(6);
numeros.pop();
numeros.shift();
numeros.unshift(0);
numeros.splice(2, 1);
const slicedArray = numeros.slice(1, 3);
console.log(slicedArray, 'slicedArray');
const indexOfTwo = numeros.indexOf(2);
console.log(indexOfTwo, 'indexOfTwo');
const mappedArray = numeros.map((num) => num * 2);
console.log(mappedArray, 'mappedArray');
const filteredArray = numeros.filter((num) => num > 2);
console.log(filteredArray, 'filteredArray');
const reducedArray = numeros.reduce((acc, num) => acc + num, 0);
console.log(reducedArray, 'reducedArray');
const text = 'Juan, Camilo, Perez';
const splittedText = text.split(',');
console.log(splittedText, 'splittedText');
const numbers = [1, 2, 3, 4, 5];
const joinedNumbers = numbers.join(', ');
console.log(joinedNumbers, 'joinedNumbers');
const foundNumber = numbers.find((num) => num === 3);
console.log(foundNumber, 'foundNumber');
const foundIndex = numbers.findIndex((num) => num === 3);
console.log(foundIndex, 'foundIndex');
const everyNumber = numbers.every((num) => num > 0);
console.log(everyNumber, 'everyNumber');
const someNumber = numbers.some((num) => num > 3);
console.log(someNumber, 'someNumber');
const array2 = [6, 7, 8];
const concatenatedArray = numbers.concat(array2);
console.log(concatenatedArray, 'concatenatedArray');
const unsortedArray = [5, 3, 1, 4, 2];
const sortedArray = unsortedArray.sort((a, b) => a - b);
console.log(sortedArray, 'sortedArray');
const sortedArray2 = [1, 2, 3, 4, 5];
const reversedArray = sortedArray2.reverse();
console.log(reversedArray, 'reversedArray');
const numbers1 = [1, 2, 3, 4, 5];
const includedNumber = numbers1.includes(3);
console.log(includedNumber, 'includedNumber');
//# sourceMappingURL=arrays.js.map